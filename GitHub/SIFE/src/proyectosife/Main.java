package proyectosife;

import javax.swing.JOptionPane;

public class Main {

    /**
     * @param args the command line arguments
     */
  /*  public static void main(String[] args) {
        String nombre; String apellido; String correoElectronico; 
            String direccion; int idPersona; int telefono; int tipoCliente;
            cCliente llama = new cCliente("","","","",0,0,0);
        nombre = JOptionPane.showInputDialog(null, "Digete el valor de su lado: ");
        apellido = JOptionPane.showInputDialog(null, "Digete el valor de su lado: ");
        correoElectronico = JOptionPane.showInputDialog(null, "Digete el valor de su lado: ");
        direccion = JOptionPane.showInputDialog(null, "Digete el valor de su lado: ");
        idPersona = Integer.parseInt(JOptionPane.showInputDialog(null, "Digete el valor de su lado: "));
        telefono = Integer.parseInt(JOptionPane.showInputDialog(null, "Digete el valor de su lado: "));
        tipoCliente = Integer.parseInt(JOptionPane.showInputDialog(null, "Digete el valor de su lado: "));
        llama.setNombre(nombre);
        llama.setApellido(apellido);
        llama.setCorreoElectronico(correoElectronico);
        llama.setDireccion(direccion);
        llama.setIdPersona(idPersona);
        llama.setTelefono(telefono);
        llama.setTipocliente(tipoCliente);
        llama.agregar(llama.getNombre(), llama.getApellido(), llama.getCorreoElectronico(),
                llama.getDireccion(), llama.getIdPersona(), llama.getTelefono(), llama.getTipocliente());
    }*/
    
}
